# Normalize text files and let Git detect binaries.
* text=auto

# Backend files are edited mostly on Windows/Visual Studio in this repo.
back/** text=auto eol=crlf

# Shell scripts must keep LF.
*.sh text eol=lf
